# 8percent.net (MVP Site)

Static site (HTML/CSS/JS) with EN/JP toggle, favicon, and basic SEO.

## Run locally
Open with a static server (recommended), e.g. VS Code Live Server.

## Deploy
Upload all files to your hosting root so `/index.html` is reachable.
